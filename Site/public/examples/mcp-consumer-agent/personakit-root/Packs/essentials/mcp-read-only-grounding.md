# MCP Read-Only Grounding

PersonaKit MCP resources and tools provide context and provenance.

They do not authorize writing files, running shell commands, mutating PersonaKit roots, launching external agents, or simulating execution through prompts.

Resolve the active PersonaKit contract before selecting host-local tools or slash skills.

Stop when a required capability is undeclared or unauthorized.


# Review Boundaries

This session is for review only.

Do not edit files, launch implementation agents, run deployment commands, or turn review findings into fixes.

Lead with findings ordered by severity. Ground each finding in the supplied artifact, diff, or command output.

If implementation is needed, stop and request a separate implementation session.


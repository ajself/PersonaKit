{
  "id": "orbit-rerun-startup-contract",
  "version": "1.0",
  "name": "Orbit Rerun Startup Contract",
  "description": "Stage the Orbit rerun from a fresh main-based lane with explicit learning carry-forward, product gates, and a deterministic handoff into the live execution surface, while allowing startup to stay read-only until execution kickoff.",
  "parameters": [
    {
      "name": "objective",
      "type": "string",
      "required": true
    },
    {
      "name": "attemptLabel",
      "type": "string",
      "required": false
    },
    {
      "name": "laneBranch",
      "type": "string",
      "required": true
    },
    {
      "name": "evidencePacketPath",
      "type": "string",
      "required": true
    },
    {
      "name": "rerunPrepPath",
      "type": "string",
      "required": true
    },
    {
      "name": "executionSessionId",
      "type": "string",
      "required": true
    },
    {
      "name": "participantEvidencePath",
      "type": "string",
      "required": true
    },
    {
      "name": "productAcceptancePath",
      "type": "string",
      "required": true
    },
    {
      "name": "interactionReviewPath",
      "type": "string",
      "required": true
    },
    {
      "name": "validationCloseoutPath",
      "type": "string",
      "required": true
    },
    {
      "name": "retrospectivePath",
      "type": "string",
      "required": true
    }
  ],
  "includesEssentialIds": [
    "orbit-rerun-playbook",
    "orbit-attempt-improvement-loop",
    "non-goals",
    "tools-and-constraints"
  ],
  "requiresSkillIds": [
    "codex-cli"
  ],
  "risk": {
    "level": "high",
    "requiresHumanReview": true,
    "notes": [
      "Use the Orbit execution checklists, rerun-prep artifact, explicit attempt artifact paths, explicit retrospective path, and declared execution session routing as the source of truth for the rerun.",
      "Carry forward prior-attempt lessons explicitly; do not restart from vibes or thread memory alone.",
      "Use the exact manifest-approved lane branch for the attempt; do not infer branch identity from the latest historical run.",
      "Treat this intent as a startup-only contract and hand the lane contract to the named execution session before substantive implementation begins.",
      "Startup may verify the lane contract from the repo root before any worktree exists; live execution must materialize and preflight the worktree before implementation starts.",
      "Record the rerun-prep path plus the attempt-specific product, interaction, validation, participant-evidence, and retrospective paths explicitly before handoff.",
      "Do not call the branch review-ready or MVP candidate before the product acceptance checklist and interaction-quality review both exist.",
      "Make actual participants and participant evidence explicit; planned roles are not the same as active contributors.",
      "Use an attempt-specific evidence packet path and preserve earlier attempt artifacts as historical evidence.",
      "Do not promote process changes or rerun success claims without AJ review."
    ]
  }
}

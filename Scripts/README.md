# Scripts

This folder contains development-only tooling that is not shipped with the app or
user-facing CLI.

## release-check.sh

Run local release checks. See `RELEASE_CHECKLIST.md` for the canonical checklist.

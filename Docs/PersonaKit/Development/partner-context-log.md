# Partner Context Log

Status: Active  
Owner: AJ  
Last Reviewed: 2026-03-10

## Purpose

Keep a durable record of AJ updates, implications, and resulting pack/session
adjustments so partner context remains coherent over time.

## Entries

| Date | Update Summary | Implications | Affected IDs | Next Action | Verification |
| --- | --- | --- | --- | --- | --- |
| 2026-03-07 | Need concrete trusted partner representation for cross-pack and subagent coordination | Added dedicated partner persona stack and sync session, then renamed active partner identity to Samwise | `samwise`, `trusted-partner-core`, `partner-sync-review`, `maintain-partner-sync-and-handoffs`, `samwise-partner-sync` | Use `samwise-partner-sync` for future partner updates and handoff planning | `personakit validate` passed |
| 2026-03-07 | Need explicit closeout-checkpoint wrap protocol with learning and continuity notes | Added Samwise closeout essential/intent/directive/session and diary JSONL contract for reflection and checkpoint re-entry readiness | `samwise`, `trusted-partner-core`, `samwise-daily-closeout`, `run-samwise-daily-closeout`, `samwise-diary.jsonl` | Run `samwise-daily-closeout` at closeout checkpoints and append one diary entry | `personakit validate` passed; schema + log files created |
| 2026-03-07 | Require explicit AJ consent for every single git commit | Added hard per-commit authorization rule across Samwise persona, trust contract, and partner directives/intents | `samwise`, `partner-trust-contract`, `maintain-partner-sync-and-handoffs`, `partner-sync-review`, `run-samwise-daily-closeout` | Ask AJ for explicit approval before each commit; treat each commit as separately gated | `personakit validate` passed |
| 2026-03-07 | Allow standing commit permission only in dedicated non-main worktrees with AJ auto-commit approval | Refined commit policy so Persona/Pack/Session commit authority is denied by default and may only be enabled by scoped worktree approval | `samwise`, `partner-trust-contract`, `maintain-partner-sync-and-handoffs`, `partner-sync-review`, `run-samwise-daily-closeout`, `tools-and-constraints`, `non-goals` | Check worktree and approval scope before commits; if scope is missing or `main`, fall back to explicit per-commit AJ approval | `personakit validate` passed |
| 2026-03-07 | Need a reusable Gardening v2 standard from today’s lessons | Added a deterministic checklist and wired it into pack-gardener standards/kit/intent/directive so each gardening pass follows the same quality loop | `gardening-v2-checklist`, `pack-gardening-standards`, `pack-gardener-core`, `pack-maintenance-review`, `tend-packs-and-sessions` | Use checklist on every pack/session maintenance pass and record pass status in logs | `personakit validate` passed |
| 2026-03-07 | Need explicit guidance that gardener can garden itself without bypassing controls | Clarified self-gardening behavior so gardener-owned artifact updates require the same analysis-first and human approval gates | `gardening-v2-checklist`, `pack-gardening-standards`, `tend-packs-and-sessions`, `pack-maintenance-review` | Label self-gardening passes explicitly and keep approval parity with all other gardening changes | `personakit validate` passed |
| 2026-03-07 | Need explicit gardener identity naming convention for readability | Set pack gardener persona display name to `Rosie` while keeping stable persona ID `pack-gardener` for compatibility | `pack-gardener` | Use persona IDs for machine references and names for human-facing context | `personakit validate` passed |
| 2026-03-07 | AJ requested Samwise startup brief, then approved startup-context first action | Active working session is `samwise-partner-sync`; reserve `samwise-daily-closeout` for explicit closeout checkpoints and keep execution bounded pending review stop points | `samwise`, `trusted-partner-core`, `samwise-partner-sync`, `maintain-partner-sync-and-handoffs`, `samwise-daily-closeout` | Keep updates in logs, define explicit handoff scope, and pause for AJ review before broad multi-lane execution | PersonaKit MCP catalog + session trace/export reviewed; partner context and handoff register updated |
| 2026-03-07 | AJ confirmed dedicated Rosie worktree policy for ongoing gardening execution | Standing commit authority is approved for this exact non-`main` scope (`/Users/ajself/.codex/worktrees/7ac0/PersonaKit`, branch `rosies-garden`); commits happen here, then are rebased onto `main` in the main worktree; this worktree remains persistent and rebases `main` back in for future runs | `samwise`, `pack-gardener`, `trusted-partner-core`, `samwise-partner-sync`, `maintain-partner-sync-and-handoffs` | Run regular Rosie gardening tasks in this worktree, commit here under approved scope, and keep rebases explicit and traceable in logs | AJ confirmation recorded in-thread; branch/worktree scope verified via git |
| 2026-03-07 | Need first-class Rosie upkeep workflow artifacts for lane/main sync routine | Added dedicated Rosie worktree upkeep essential + intent + directive + session and wired them into gardener defaults so the branch/worktree model is executable and review-gated | `rosie-worktree-upkeep-standards`, `rosie-worktree-upkeep-review`, `maintain-rosie-worktree-upkeep-loop`, `rosie-worktree-upkeep`, `pack-gardener-core`, `PSG-012` | Use `rosie-worktree-upkeep` for recurring Rosie cycles; pause for AJ review before any history-altering lane-to-main integration | `personakit validate` passed; gardening and partner logs updated |
| 2026-03-07 | Rosie self-gardening audit found policy-scope drift in new upkeep stack | Applied approved alignment fixes: removed lane-specific essential from global gardener kit, tightened directive lane-to-main wording to main worktree, required explicit branch parameters, and aligned Rosie persona responsibilities/non-goals with lane/main gates | `pack-gardener-core`, `maintain-rosie-worktree-upkeep-loop`, `rosie-worktree-upkeep-review`, `pack-gardener`, `PSG-013` | Use `rosie-worktree-upkeep` with explicit `laneBranch` and `integrationBranch`; keep mainline integration review-gated | Subagent audit findings applied; validation and gardening logs updated |
| 2026-03-07 | AJ requested Rosie persona-focused gardening pass after upkeep alignment commit | Audit found no high-severity drift; applied low-severity wording hardening so commit authorization mode and wrong-lane constraints are explicit in local directive/persona text | `maintain-rosie-worktree-upkeep-loop`, `pack-gardener`, `PSG-014` | Continue Rosie upkeep cycles with explicit guardrail wording and existing review gates | `personakit validate` passed; gardening logs updated |
| 2026-03-07 | Need a repeatable reverse-interview workflow to qualify persona candidates and expose hiring gaps | Added Samwise hiring stack artifacts: `persona-hiring-standards` essential, `reverse-interview-persona-fit` intent, `reverse-interview-persona-candidate` directive, and `samwise-persona-hiring` session | `persona-hiring-standards`, `reverse-interview-persona-fit`, `reverse-interview-persona-candidate`, `samwise-persona-hiring` | Use `samwise-persona-hiring` when evaluating a persona candidate and proposing missing essentials/kits/directives/sessions | `personakit validate` passed |
| 2026-03-07 | Need dynamic confidence handling when role/domain knowledge is incomplete during reverse interviews | Added confidence rubric, low-confidence protocol, and dedicated research-loop essential so reverse interviews can gather evidence, re-score, and stop safely when uncertainty remains | `persona-hiring-standards`, `persona-hiring-research-loop`, `reverse-interview-persona-fit`, `reverse-interview-persona-candidate` | Run research loop whenever confidence is low or unknowns are material; require evidence references and post-research re-score before final verdict | `personakit validate` passed |
| 2026-03-07 | Need reverse-interview outcomes to be durable, queryable, and deterministic | Added persistence rules, default threshold behavior, hiring log contract, report directory, and JSONL/schema files for persona hiring assessments | `persona-hiring-standards`, `persona-hiring-log-contract`, `reverse-interview-persona-fit`, `reverse-interview-persona-candidate`, `Docs/PersonaKit/Development/hiring-reviews`, `Docs/PersonaKit/Development/logs/persona-hiring-reviews.*` | Always write markdown report + JSONL entry for reverse interviews; default confidence threshold to 80 when omitted | `personakit validate` passed |
| 2026-03-07 | Need concrete closure path for hiring-gap findings from Samwise self interview | Added calibration playbook, hiring core kit, remediation intent/directive/session, Samwise calibration guardrail language, and dedicated hiring-log checker script | `persona-hiring-calibration-playbook`, `persona-hiring-core`, `remediate-persona-hiring-gaps`, `close-persona-hiring-gap-loop`, `samwise-persona-hiring-calibration`, `samwise`, `Scripts/check-persona-hiring-logs.sh` | Run remediation via `samwise-persona-hiring-calibration`, then re-evaluate hiring confidence and update review artifacts | `personakit validate` passed; hiring log check passed |
| 2026-03-07 | Need to raise venture-product-steward confidence above assignment threshold | Added venture PM core kit + essentials + discovery/planning/tracking intent/directive/session surfaces, then ran strong/weak benchmark calibrations and re-score (`PHR-0003..0005`) | `venture-product-core`, `venture-product-steward`, `venture-product-principles`, `feature-brief-template`, `roadmap-milestone-tracking-standard`, `venture-product-quality-gate`, `shape-macos-feature-opportunity`, `plan-macos-feature-delivery`, `track-macos-feature-delivery`, `run-venture-product-*`, `venture-product-*`, `PHR-0003`, `PHR-0004`, `PHR-0005` | Assign pilot PM task via `venture-product-discovery`; keep quality-gate pass required before planning handoff | `personakit validate` passed; hiring/gardening log checks passed |
| 2026-03-07 | AJ requested a new Admin planning feature modeled after Trello/GitHub Issues with lane templates and `ticket` terminology | Produced bounded discovery+planning brief for Studio `Admin -> Ticket Planning`, including scope, data model draft, milestones (`ATP-M1..M4`), and quality gate result | `venture-product-steward`, `venture-product-discovery`, `venture-product-planning`, `admin-ticket-planning-feature-brief`, `SidebarItem`, `StudioRootView` | Review and approve v1 scope, then execute `ATP-M1` UI shell implementation | Feature brief drafted and indexed in plan docs |
| 2026-03-07 | Need a dedicated third persona for interaction-quality judgment before claiming Trello-level experience | Added `studio-interaction-quality-lead` persona plus `interaction-quality-core` kit, new rubric/pattern/report essentials, and red-pen intent/directive/session wiring | `studio-interaction-quality-lead`, `interaction-quality-core`, `interaction-quality-rubric`, `planning-board-ux-patterns`, `interaction-quality-report-template`, `assess-ticket-board-interaction-quality`, `run-ticket-board-red-pen-pass`, `studio-interaction-quality` | Run `studio-interaction-quality` after `ATP-M1` to score current UX and propose prioritized fixes before M2 | PersonaKit pack/session artifacts added and linked in planning docs |
| 2026-03-07 | AJ requested reverse-interview on `studio-interaction-quality-lead` | Completed hiring pass `PHR-0007`; verdict `qualified` at 84 with medium gaps focused on benchmark calibration and durable quality-log contract | `samwise-persona-hiring`, `PHR-0007`, `studio-interaction-quality-lead`, `interaction-quality-core`, `studio-interaction-quality` | Run first red-pen pass after `ATP-M1`; then add benchmark and log-contract essentials if approved | Hiring report + JSONL entry added; pending hiring-log validation |
| 2026-03-07 | AJ finalized feature naming as `Taskboard` and requested ATP-M1 start | Implemented `Admin -> Taskboard` sidebar/navigation shell, added deterministic placeholder board lanes/tickets, and wired Taskboard contextual help + storage key + tests | `TaskboardPanelView`, `SidebarItem.taskboard`, `StudioSidebarView`, `StudioRootView`, `StudioHelpCatalog`, `StudioHelpStorage`, `StudioHelpTopicID`, `StudioHelpCatalogTests`, `admin-ticket-planning-feature-brief` | Run first red-pen pass with `studio-interaction-quality` on the shell, then scope `ATP-M2` lane CRUD | `swift test --filter StudioHelpCatalogTests` passed; `swift test --filter StudioRootNavigationStateTests` passed; `personakit validate` passed |
| 2026-03-07 | Continue ATP loop after Taskboard M1 shell | Ran first `studio-interaction-quality` red-pen pass for ATP-M1 shell; result `stop` with score `47/100` due expected blocker gaps (missing lane/ticket CRUD and move flows) | `taskboard-atp-m1-red-pen-review`, `studio-interaction-quality`, `admin-ticket-planning-feature-brief`, `ATP-M1`, `ATP-M2` | Execute ATP-M2 lane template + ticket creation flows, then rerun red-pen pass | Interaction report drafted and linked; ATP brief updated with M1 done + interaction gate outcome |
| 2026-03-07 | Continue ATP loop with M2 implementation slice | Implemented lane template add/edit/delete/reorder, ticket creation, and workspace-local persistence for Taskboard, then ran second red-pen pass | `TaskboardPanelView`, `ATP-M2`, `taskboard-atp-m2-red-pen-review`, `studio-interaction-quality`, `admin-ticket-planning-feature-brief` | Execute ATP-M3 ticket edit/delete/move, then rerun red-pen pass | `swift test --filter StudioHelpCatalogTests` passed; `swift test --filter StudioRootNavigationStateTests` passed; `xcodebuildmcp macos build` succeeded; M2 red-pen score `69/100` (`stop`) |
| 2026-03-07 | Continue ATP loop through M3/M4 stabilization | Implemented ticket edit/delete/move flows, added empty-board state, reduced persistence message noise, and ran third red-pen pass | `TaskboardPanelView`, `ATP-M3`, `ATP-M4`, `taskboard-atp-m3-red-pen-review`, `admin-ticket-planning-feature-brief` | Use current Taskboard as pilot baseline; queue parity-polish follow-up for drag/drop and keyboard acceleration | `swift test --filter StudioHelpCatalogTests` passed; `swift test --filter StudioRootNavigationStateTests` passed; `xcodebuildmcp macos build` succeeded; M3 red-pen score `77/100` (`go-with-notes`) |
| 2026-03-07 | Continue beyond ATP with parity polish pass 1 | Added lane selection model, quick keyboard actions, drag-and-drop ticket movement, and selection continuity; then ran parity review pass | `TaskboardPanelView`, `taskboard-parity-polish-pass-1`, `admin-ticket-planning-feature-brief` | Decide whether to run one more minor polish pass for `>= 85` or treat current build as pilot-ready baseline | `swift test --filter StudioHelpCatalogTests` passed; `swift test --filter StudioRootNavigationStateTests` passed; `xcodebuildmcp macos build` succeeded; parity pass score `84/100` (`go-with-notes`) |
| 2026-03-07 | Continue parity polish with drop-target feedback pass | Added active drop-target lane highlighting for drag/drop ticket movement and reran interaction-quality assessment to close parity threshold gap | `TaskboardPanelView`, `taskboard-parity-polish-pass-2`, `admin-ticket-planning-feature-brief` | Treat Taskboard as parity-ready pilot baseline and gather usage feedback | `swift test --filter StudioHelpCatalogTests` passed; `swift test --filter StudioRootNavigationStateTests` passed; `xcodebuildmcp macos build` succeeded; parity pass score `86/100` (`go`) |
| 2026-03-07 | AJ clarified that Samwise closeout timing is checkpoint-based, not literal calendar day boundaries | Updated Samwise and partner governance artifacts so `samwise-daily-closeout` is explicitly a flexible pause/reflection checkpoint workflow | `samwise`, `samwise-daily-closeout`, `run-samwise-daily-closeout`, `maintain-partner-sync-and-handoffs`, `partner-trust-contract`, `TODO`, `closeout-checklist` | Keep using checkpoint closeouts whenever we pause and reflect, regardless of clock-time day boundaries | `personakit validate` passed; gardening log checks passed |
| 2026-03-07 | AJ requested a neutral “coffee” thematic startup mode before execution | Added Samwise coffee-checkpoint essential, intent, directive, and session so startup can begin with friendly orientation and explicit track choices (Swift updates, forums pulse, resume context, default work mode) | `samwise-coffee-checkpoint`, `run-samwise-coffee-checkpoint`, `samwise-coffee-checkpoint.session`, `session-directory` | Use `samwise-coffee-checkpoint` before deep execution when we want a warm, neutral startup state | `personakit validate` passed; gardening log checks passed |
| 2026-03-07 | Coffee-checkpoint red-pen pass requested for critical quality review | Hardened coffee-checkpoint semantics to improve execution safety and clarity: added external-update fallback contract, aligned intent review gating, and clarified expected startup track values | `samwise-coffee-checkpoint`, `run-samwise-coffee-checkpoint`, `samwise-coffee-checkpoint.intent` | Use explicit track IDs and require fallback messaging when live external signal verification is unavailable | `personakit validate` passed; gardening log checks passed |
| 2026-03-07 | AJ requested a Taskboard next-version plan focused on real usefulness over parity claims | Added Taskboard v2 initiative plan with early AI-editable board contract, Trello research lane (web + published images), and snapshot-testing lane for visual quality guardrails | `taskboard-v2-initiative-plan`, `TODO`, `Plan README`, `taskboard`, `studio-interaction-quality`, `venture-product-steward` | Run TV2-M1 foundation gate and lock AI contract + research + snapshot scaffolding before deep implementation | Planning artifacts updated; execution queued in TODO |
| 2026-03-07 | AJ clarified sequencing: define expected Trello features only after rigorous research, and before any feature implementation | Refined Taskboard v2 plan with a mandatory AJ feature-lock checkpoint (`G3.5`) and explicit multiagent lane ownership; added hard no-build-before-lock rule | `taskboard-v2-initiative-plan`, `TODO`, `taskboard-v2-feature-lock` | Complete research corpus, then freeze must-have/should-have/later list in feature-lock doc before implementation starts | Plan constraints updated and queued as top-priority gate |
| 2026-03-07 | AJ requested a concrete Rosie gardening-tools plan and immediate continuation | Added phased roadmap plus phase-1 observability spine (health snapshots + recommendation feedback) and wired the telemetry contract into gardener standards and upkeep flow | `gardening-tools-roadmap`, `gardening-health-snapshots.*`, `gardening-recommendation-feedback.*`, `gardening-health-snapshot-contract`, `gardening-recommendation-feedback-contract`, `pack-gardener-core`, `pack-maintenance-review`, `tend-packs-and-sessions`, `PSG-015`, `PSG-016`, `PSG-017` | Execute phase 2 next: deterministic recommendation scoring rubric with stable `GREC-*` explanations | `personakit validate` passed; gardening log checks passed |
| 2026-03-07 | AJ requested immediate continuation into phase-2 recommendation engine | Added deterministic `GREC-*` scoring contract and ranked recommendation stream with score formula, explanation fields, and validator enforcement | `gardening-recommendation-scoring-contract`, `gardening-recommendations.*`, `check-gardening-logs`, `pack-gardener-core`, `pack-maintenance-review`, `tend-packs-and-sessions`, `PSG-018` | Execute phase 3/4 next using ranked `GREC-*` outputs (`PSG-019`, `PSG-020`) | `personakit validate` passed; gardening log checks passed |
| 2026-03-07 | AJ asked Rosie to garden while we paused for coffee | Completed phase-3/4 gardening toolchain by adding coverage auditing plus policy-conflict and safety-preflight detectors with validator-enforced fail-fast rules | `pack-coverage-auditor-contract`, `policy-conflict-detector-contract`, `self-gardening-safety-preflight-contract`, `gardening-pack-coverage.*`, `gardening-policy-conflicts.*`, `gardening-safety-preflight.*`, `check-gardening-logs`, `PSG-019`, `PSG-020` | Run ongoing upkeep using full phase-1..4 detector/log suite and escalate only when checks flag drift/conflicts | `personakit validate` passed; gardening/hiring log checks passed |
| 2026-03-07 | AJ approved start of Taskboard v2 execution with research-first and AI-editability constraints | Completed TV2-M1 planning artifacts for Trello benchmark corpus, image catalog, gap matrix, AI mutation contract draft, and snapshot lane spec while keeping feature implementation blocked pending `G3.5` feature lock | `taskboard-v2-initiative-plan`, `taskboard-v2-feature-lock`, `taskboard-ai-mutation-contract`, `taskboard-v2-snapshot-lane`, `Docs/Research/*`, `TODO` | Run AJ feature-lock review and contract signoff, then start implementation only after approval | Planning artifacts created and indexed; queue updated in TODO |
| 2026-03-07 | AJ approved the Taskboard v2 priority model and requested immediate execution start | Locked `G3.5` feature scope as `P0` -> `P1` -> `P2` -> `P3`, moved TODO queue to `TV2-M2A` execution, and opened implementation start path | `taskboard-v2-feature-lock`, `taskboard-v2-initiative-plan`, `TODO` | Execute `TV2-M2A` (labels + due date + checklist + filtering), then proceed to `TV2-M2B` | Priority model recorded and implementation queue activated |
| 2026-03-07 | AJ requested immediate continuation after Taskboard refactor checkpoints | Locked `W1` AI mutation contract decisions, updated initiative/TODO state to mark `M2A` kickoff complete, and queued mutation-interface implementation + snapshot lane as next execution steps | `taskboard-ai-mutation-contract`, `taskboard-v2-initiative-plan`, `TODO` | Implement mutation interface with deterministic tests, then land snapshot suite scaffolding | Plan artifacts updated to reflect current execution state |
| 2026-03-07 | AJ asked to continue immediately; Taskboard mutation contract moved from plan to code | Added Taskboard mutation engine request/operation/error types and deterministic application logic, plus contract-focused test coverage for determinism, revision conflict, referential integrity, idempotency, and lane-order normalization | `TaskboardMutationEngine`, `TaskboardMutationEngineTests`, `taskboard-ai-mutation-contract`, `TODO`, `taskboard-v2-initiative-plan` | Start snapshot lane scaffolding and baseline capture policy implementation | `swift test` passed (260 tests); `xcodebuildmcp macos build` succeeded |
| 2026-03-07 | AJ asked to continue immediately; completed Taskboard v2 M2A/M2B baseline integration pass | Landed deterministic Taskboard mutation engine + tests, activated snapshot lane with default/empty baselines, and implemented P1 keyboard/search baseline with deterministic search/navigation helpers | `TaskboardMutationEngine`, `TaskboardMutationEngineTests`, `TaskboardSearchEngine`, `TaskboardSearchEngineTests`, `TaskboardSnapshotTests`, `taskboard-v2-initiative-plan`, `taskboard-v2-snapshot-lane`, `TODO` | Expand snapshot coverage to remaining required scenarios (`7/7`) and then execute P2 assignment/description depth slice | `swift test` passed (265 tests); `xcodebuildmcp macos build --workspace-path PersonaKit.xcworkspace --scheme PersonaKitStudio` succeeded |
| 2026-03-07 | AJ asked to keep improving despite parity gap; continue execution after baseline commit | Implemented Taskboard depth and lane-control uplift to move beyond shallow card metadata: multi-assignee tickets, markdown description/comments, WIP limits, lane collapse, and refreshed visual baselines | `TaskboardModels`, `TaskboardDrafts`, `TaskboardPanelView+UI`, `TaskboardPanelView+Actions`, `TaskboardMutationEngine`, `TaskboardSearchEngine`, `TaskboardSnapshotTests`, `TODO`, `taskboard-v2-initiative-plan` | Run next interaction-parity hardening slice (in-lane reorder + lower click depth) and expand snapshot coverage toward `7/7` required scenarios | `swift test` passed (265 tests); `xcodebuildmcp macos build --workspace-path PersonaKit.xcworkspace --scheme PersonaKitStudio` succeeded |

| 2026-03-08 | Approved Taskboard Trello-parity squad experiment with Samwise delegated commit authority inside the active initiative worktree | Added explicit board-and-card parity charter, staffed squad-lead delivery model, and initiative-scoped `samwise-feature-commit-approved` authorization recorded for branch `codex/night-shift` in `/Users/ajself/Code/PersonaKit` | `taskboard-trello-parity-execution-charter`, `worktree-squad-lead`, `worktree-squad-core`, `worktree-squad-delivery`, `samwise-worktree-squad-oversight`, `studio-swiftui-product-engineer`, `taskboard-parity-designer`, `taskboard-board-card-build`, `taskboard-parity-design-review` | Use Samwise as commit approver only inside this initiative scope, keep AJ as release manager for `main`, and run a retrospective on the experiment before any broader rollout | Partner trust contract, squad contracts, and plan docs updated; `personakit validate`, hiring-log check, and squad-log check passed |
| 2026-03-08 | Need Taskboard visual evidence to cover the full parity baseline without relying on memory or fake sheet snapshots | Added a snapshot-seed seam for transient board state, expanded Taskboard snapshots from `2/7` to `7/7`, and switched editor-open baselines to a board-plus-editor harness because plain macOS `.sheet` presentation is not captured by `NSHostingView` image snapshots | `TaskboardPanelView`, `TaskboardSnapshotTests`, `taskboard-v2-snapshot-lane`, `night-shift-taskboard-rival-plan`, `taskboard-v2-initiative-plan`, `TODO` | Treat snapshot matrix as the parity visual regression lane while moving on to `NS0` evidence/report loop and `NS1` speed work | `swift test --filter TaskboardSnapshotTests`, `swift test`, and `xcodebuildmcp macos build --workspace-path PersonaKit.xcworkspace --scheme PersonaKitStudio` passed |
| 2026-03-08 | Need a lower-friction card editing loop that feels closer to Trello than opening a full sheet for small metadata changes | Added inline quick edit for title, assignees, and labels directly on cards, plus a dedicated visual baseline so the faster editing surface is locked into parity review | `TaskboardPanelView`, `TaskboardDrafts`, `TaskboardPanelView+Actions`, `TaskboardPanelView+UI`, `TaskboardPanelView+UIHelpers`, `TaskboardSnapshotTests`, `taskboard-v2-snapshot-lane`, `night-shift-taskboard-rival-plan`, `TODO` | Use inline quick edit as the default small-change path while continuing the `NS1` keyboard-first triage/movement slice | `swift test --filter TaskboardSnapshotTests`, `swift test`, and `xcodebuildmcp macos build --workspace-path PersonaKit.xcworkspace --scheme PersonaKitStudio` passed |
| 2026-03-08 | Need keyboard-first Taskboard speed paths to start from a concrete ticket selection model rather than loose lane-only focus | Added selected-ticket state, ticket-selection shortcuts, and a shortcut to open inline quick edit for the selected card; refreshed Taskboard visual baselines so the stronger selection model is reviewable | `TaskboardPanelView`, `TaskboardPanelView+Actions`, `TaskboardPanelView+UI`, `TaskboardPanelView+UIHelpers`, `TaskboardSnapshotTests`, `night-shift-taskboard-rival-plan`, `TODO` | Continue `NS1` by adding keyboard-driven ticket movement/handoff now that selection state exists | `swift test`, `swift test --filter TaskboardSnapshotTests`, and `xcodebuildmcp macos build --workspace-path PersonaKit.xcworkspace --scheme PersonaKitStudio` passed |
| 2026-03-08 | Need selected-ticket keyboard flow to support real lane handoff instead of stopping at selection-only state | Added keyboard handoff left/right for the selected ticket, kept selection attached to the moved card, and added a dedicated selected-ticket visual baseline so the new focus state is visible in review | `TaskboardPanelView+Actions`, `TaskboardPanelView+UI`, `TaskboardSnapshotTests`, `night-shift-taskboard-rival-plan`, `TODO` | Continue `NS1` with in-lane keyboard reordering and keep NS0 honest by generating a real interaction report artifact rather than synthetic data | `swift test --filter TaskboardSnapshotTests`, `swift test`, and `xcodebuildmcp macos build --workspace-path PersonaKit.xcworkspace --scheme PersonaKitStudio` passed |
| 2026-03-08 | Need keyboard-first movement to cover in-lane reordering as well as cross-lane handoff before P2 can shift to polish | Added keyboard reordering up/down for the selected ticket within its lane, keeping the same selected-card model and refreshed visual baselines aligned with the expanded header controls | `TaskboardPanelView+Actions`, `TaskboardPanelView+UI`, `TaskboardSnapshotTests`, `taskboard-v2-snapshot-lane`, `night-shift-taskboard-rival-plan`, `TODO` | Treat the keyboard-first movement loop as baseline-complete and shift remaining P2 focus to interaction polish plus the still-open NS0 evidence/report loop | `swift test --filter TaskboardSnapshotTests`, `swift test`, and `xcodebuildmcp macos build --workspace-path PersonaKit.xcworkspace --scheme PersonaKitStudio` passed |
| 2026-03-08 | Need the board to feel calmer at first glance now that keyboard movement exists, instead of advertising every control in the header | Collapsed the top command strip into primary board actions plus a single overflow actions menu, keeping keyboard shortcuts intact while reducing default command density in the visible board chrome | `TaskboardPanelView+UI`, `TaskboardSnapshotTests`, `night-shift-taskboard-rival-plan`, `TODO` | Continue P2 with additional board/card polish and keep the still-open NS0 evidence loop honest about missing real interaction artifacts | `swift test --filter TaskboardSnapshotTests`, `swift test`, and `xcodebuildmcp macos build --workspace-path PersonaKit.xcworkspace --scheme PersonaKitStudio` passed |
| 2026-03-08 | Need card metadata to scan more like Trello at a glance now that the board chrome is calmer | Replaced the plain-text label line on cards with deterministic colored label chips, capped visible chips to three plus overflow count, and refreshed the snapshot matrix so dense lanes read faster without adding randomness | `TaskboardPanelView+UI`, `TaskboardPanelView+UIHelpers`, `TaskboardSnapshotTests`, `night-shift-taskboard-rival-plan`, `TODO` | Continue P2 with deeper card/detail polish and keep the still-open NS0 evidence loop honest about missing real interaction artifacts | `swift test --filter TaskboardSnapshotTests/testTaskboardSelectedLane`, `swift test`, and `xcodebuildmcp macos build --workspace-path PersonaKit.xcworkspace --scheme PersonaKitStudio` passed |
| 2026-03-08 | Need cards to behave more like Trello cards and less like mini control strips | Removed the always-visible quick-edit and lane-move icon cluster from card headers, kept one overflow menu for secondary actions, and wired double-click on a card to open the full ticket editor while preserving single-click selection for keyboard workflows | `TaskboardPanelView+Actions`, `TaskboardPanelView+UI`, `TaskboardSnapshotTests`, `night-shift-taskboard-rival-plan`, `TODO` | Continue P2 with deeper card/detail polish and keep the still-open NS0 evidence loop honest about missing real interaction artifacts that require a real interactive app run | `swift test --filter TaskboardSnapshotTests`, `swift test`, and `xcodebuildmcp macos build --workspace-path PersonaKit.xcworkspace --scheme PersonaKitStudio` passed |
| 2026-03-08 | Need the default board to stay calm even after making card detail easier to open | Hid the remaining overflow action menu on unselected cards, keeping it visible only for the selected or inline-edited ticket so the board reads more like Trello while preserving direct action access on the active card | `TaskboardPanelView+UI`, `TaskboardSnapshotTests`, `night-shift-taskboard-rival-plan`, `TODO` | Continue P2 with deeper card/detail polish and keep the still-open NS0 evidence loop honest about missing real interaction artifacts that require a real interactive app run | `swift test --filter TaskboardSnapshotTests`, `swift test`, and `xcodebuildmcp macos build --workspace-path PersonaKit.xcworkspace --scheme PersonaKitStudio` passed |
| 2026-03-08 | Need the active docs to stay honest about what automation can and cannot do from this environment | Confirmed the `NS0` telemetry/report loop is code-complete but still requires a real interactive app session with a loaded workspace, and captured that snapshot record mode still hits a user-only approval boundary that belongs in the delegated-authority retrospective | `TaskboardNightShiftTelemetry`, `TaskboardPanelView+Persistence`, `TaskboardPanelView+UI`, `night-shift-taskboard-rival-plan`, `TODO` | Keep advancing non-blocked parity slices, but do not claim `NS0` complete until real interaction artifacts exist on disk | Code-path audit completed; snapshot-record approval interruption observed during execution |
| 2026-03-09 | Need a pause-safe handoff after the paired `NS0` run changed from fully blocked to only partially complete | Switched the app target to read-write user-selected-file access, then confirmed a real `make run` + workspace-picker session now writes `.personakit/Taskboard/taskboard.json` and emits `.personakit/Taskboard/night-shift/interaction-events.jsonl` for real `createTicket` events (`One`, `Two`), but `interaction-report.md` is still missing and the visible app chrome did not match the latest Taskboard header build | `PersonaKit.xcodeproj`, `TODO`, `night-shift-taskboard-rival-plan`, `.personakit/Taskboard/taskboard.json`, `.personakit/Taskboard/night-shift/interaction-events.jsonl` | Pause `NS0` here; when resuming, first verify `make run` launches the latest Taskboard build, then complete the remaining edit/move/collapse/report interaction loop | `make build` passed; repo-local Taskboard JSON and interaction-events artifacts were written during paired manual run |
| 2026-03-09 | Need a stable role title that fits both AJ and agents using Samwise | Standardized Samwise as the `Trusted Partner` role, with `AJ Trusted Partner` as the canonical agent-facing label, and aligned core partner docs around that framing while preserving stable PersonaKit IDs | `samwise`, `trusted-partner-core`, `partner-trust-contract`, `samwise-partner-sync`, `session-directory`, `worktree-squad-cheat-sheet` | Use `Trusted Partner` in human-facing docs, use `AJ Trusted Partner` when agents need a stable role label, and keep `samwise` as the persona/session ID anchor | `personakit validate` passed; `./Scripts/check-gardening-logs.sh` failed because existing pack-coverage snapshots are stale relative to current pack counts |
| 2026-03-09 | Need meeting-note rules to live above Samwise so transcript discipline is shared and repeatable | Added shared Orbit meeting protocol pack artifacts so meeting-note behavior depends on a reusable kit and transcript protocol instead of Samwise memory | `orbit-meeting-core`, `orbit-meeting-transcript-protocol` | Use `orbit-meeting-core` when wiring future Orbit meeting directives or sessions; always re-read `_template.md` before editing meeting notes | `swift run personakit validate --root .personakit` passed; `./Scripts/check-gardening-logs.sh` failed because pack-coverage snapshots are stale relative to current pack counts |
| 2026-03-09 | Need Samwise to form squads, identify missing roles, and produce the first plan before execution without hard-coding one workspace | Added a generic squad-planning contract, intent, directive, and session so Samwise can assemble a multiagent team, trigger reverse-interviews for uncertain roles, and stop cleanly before execution handoff | `multiagent-squad-planning-contract`, `assemble-squad-and-plan-review`, `assemble-squad-and-plan`, `samwise-squad-planning`, `session-directory` | Use `samwise-squad-planning` when a new objective needs staffing, role-coverage review, and a bounded first plan before coding or delegation begins | `swift run personakit validate --root .personakit` passed |
| 2026-03-09 | Need Samwise to preserve squad-planning results, close missing-role gaps explicitly, and carry one reusable orchestration kit across workspaces | Added a durable planning-report/log contract, a named `samwise-squad-planning-remediation` loop that routes missing personas through reverse-interview and hiring discipline, and a reusable orchestration kit spanning planning, hiring, oversight, and retrospective expectations | `squad-planning-log-contract`, `squad-planning-reviews.*`, `assemble-squad-and-plan-review`, `assemble-squad-and-plan`, `remediate-squad-role-coverage-gaps`, `close-squad-role-coverage-gap-loop`, `samwise-squad-planning-remediation`, `samwise-orchestration-core`, `samwise`, `session-directory` | Use `samwise-squad-planning` for the first pass on a brief, then route any execution-critical uncovered roles through `samwise-squad-planning-remediation` before handing off to execution | `swift run personakit validate --root .personakit`, `./Scripts/check-persona-hiring-logs.sh`, and `./Scripts/check-squad-planning-logs.sh` passed; `./Scripts/check-gardening-logs.sh` still fails because pack-coverage snapshots are stale relative to current pack counts |
| 2026-03-09 | Need “Hey Samwise, time for coffee” to behave like a real wake-up ritual for new threads | Tightened coffee-checkpoint behavior so coffee-without-a-track defaults to `resume-context` when continuity artifacts exist, and `resume-context` now reads the latest Samwise diary entry, relevant planning review, and current brief before recommending a first action | `samwise-coffee-checkpoint`, `run-samwise-coffee-checkpoint`, `samwise-daily-closeout`, `multiagent-squad-planning-contract`, `squad-planning-log-contract` | Use coffee mode as the lightweight new-thread wake-up; if a current brief is known, pass it or let Samwise infer it from the latest continuity artifacts before pausing for confirmation | `swift run personakit validate --root .personakit` passed; `./Scripts/check-gardening-logs.sh` still fails because pack-coverage snapshots are stale relative to current pack counts |
| 2026-03-09 | Need Orbit continuity to stop carrying a false-red gardening blocker into squad planning | Refreshed the append-only gardening coverage snapshot so continuity artifacts now reflect current pack/session counts instead of the stale pre-Orbit snapshot | `gardening-pack-coverage.jsonl`, `gardening-events.jsonl`, `pack-gardener-log` | Resume Orbit squad setup and product-plan definition with gardening telemetry back to green | `./Scripts/check-gardening-logs.sh` passed |
| 2026-03-09 | Need Samwise to trust AJ-approved worktree lanes after AJ manually creates them in the Codex UI | Added a machine-readable lane approval manifest plus bootstrap and preflight scripts so AJ can create a named worktree once and Samwise can later verify whether standing authority applies in that exact lane | `worktree-lane-approvals.json`, `worktree-lane-approvals.md`, `check-worktree-lane.sh`, `bootstrap-worktree-lane.sh`, `worktree-squad-cheat-sheet`, `Development/README` | After AJ creates a named approved worktree, run the lane bootstrap script inside it and rely on the lane preflight script before using standing worktree authority | `./Scripts/check-worktree-lane.sh` correctly failed on `main`; `./Scripts/check-worktree-lane.sh --branch codex/orbit-foundation` passed; `./Scripts/bootstrap-worktree-lane.sh --branch codex/orbit-foundation --output /tmp/orbit-foundation-lane.md` passed |
| 2026-03-09 | Need the lane-approval model to detect stale notes and self-validate before Samwise trusts it | Tightened the lane tooling so executable-lane preflight fails when the generated note is missing or stale, added a manifest validator, and documented the AJ-in-Codex flow more explicitly | `check-worktree-lane.sh`, `bootstrap-worktree-lane.sh`, `check-worktree-lane-approvals.sh`, `worktree-lane-approvals.md`, `worktree-squad-cheat-sheet`, `Development/README` | Run the manifest validator after approval-record edits, then bootstrap and preflight each approved worktree before relying on standing authority | `./Scripts/check-worktree-lane-approvals.sh` passed; `./Scripts/check-worktree-lane.sh` correctly failed for missing/stale lane notes until bootstrap regenerated them |
| 2026-03-09 | Need the Orbit MVP lane to map into the real Studio codebase before implementation begins | Added a first-checkpoint implementation breakdown that maps Orbit MVP work onto current Studio app entry, navigation, workspace store, feature-local persistence, and the likely session-preview seam for participant response bridging | `Orbit-First-Checkpoint-Implementation-Breakdown`, `Orbit-Execution-Plan`, `Orbit-First-Checkpoint-Runtime-Model`, `Orbit-Foundation-Lane` | Start implementation with Orbit runtime models, deterministic `.personakit/Orbit/` persistence, and an Orbit panel placeholder before committing to the heavier response bridge | Lane bootstrap and preflight passed on `codex/orbit-foundation`; source seams reviewed in Studio app, store, operation runner, taskboard persistence, and session preview manager |
| 2026-03-09 | Need the Orbit MVP lane to become a usable command-center checkpoint instead of a planning-only branch | Implemented the first Orbit Studio surface with durable workspace/thread storage, founding-group roster, participant addressing, deterministic Samwise and ProdDoc response bridging, activation traces, and new Orbit model plus snapshot coverage | `OrbitPanelView`, `OrbitModels`, `OrbitParticipantResponseBridge`, `OrbitSnapshotTests`, `OrbitWorkspaceTests`, `StudioRootView`, `StudioSidebarView`, `StudioHelpCatalog`, `TaskboardPanelView+Persistence`, `Orbit-First-Checkpoint-Implementation-Breakdown` | Treat `codex/orbit-foundation` as the official MVP candidate branch for AJ review; only move to `codex/orbit-learning-loop` after AJ reviews the foundation checkpoint | `swift build`, `swift test --filter OrbitWorkspaceTests`, `swift test --filter OrbitSnapshotTests`, and `swift test` passed; Orbit snapshot baselines recorded in `Tests/Features/Studio/__Snapshots__/OrbitSnapshotTests/` |
| 2026-03-09 | Need Samwise-led retrospectives to sort mixed product/process outcomes more rigorously than a generic “went well / didn’t” wrap-up | Codified Starfish as the default retrospective format for Samwise/worktree-squad loops, added a dedicated format essential, and updated the retrospective template plus intent/directive language to require the five Starfish buckets unless AJ asks for a different style | `starfish-retrospective-format`, `worktree-squad-retrospective-template`, `run-worktree-squad-retrospective`, `worktree-squad-retrospective-review`, `samwise-orchestration-core`, `worktree-squad-core` | Use Starfish by default for Orbit and other Samwise-led retrospectives so we can separate keep/less/more/stop/start decisions across feature quality and process quality | `swift run personakit validate --root .personakit` passed |
| 2026-03-09 | Need PersonaKit retrospective contracts to reflect the Orbit method test rather than treating retrospectives as generic wrap-up notes | Tightened the shared worktree-squad retrospective contract, schema, validator, Samwise/worktree-squad pack guidance, and development docs so new retrospectives must record method, declared vs actual participation, participant evidence, and separate feature/product/process confidence; `hybrid` is now the default closeout flow when persona fidelity or process quality is under review | `worktree-squad-loop-log-contract`, `worktree-squad-retrospectives.schema.json`, `check-worktree-squad-logs.sh`, `worktree-squad-retrospective-template`, `run-worktree-squad-retrospective`, `worktree-squad-retrospective-review`, `samwise-orchestration-core`, `worktree-squad-core`, `worktree-squad-cheat-sheet`, `Development/README`, `retrospectives/README`, `logs/README` | Use the stricter retrospective contract for future closeouts and require actual participant evidence before claiming multiagent process success | `./Scripts/check-worktree-squad-logs.sh` passed; `swift run personakit validate --root .personakit` passed |
| 2026-03-09 | Need Orbit reruns to be reachable by Samwise as a first-class PersonaKit workflow and to improve explicitly from one attempt to the next | Added an Orbit-specific rerun playbook, attempt-improvement essential, Samwise rerun directive/intent/session, and Orbit doc references so fresh `main`-based reruns begin from one named PersonaKit surface and must carry forward prior-attempt lessons explicitly | `orbit-build-rerun-playbook`, `orbit-attempt-improvement-loop`, `orbit-build-rerun-core`, `orbit-build-rerun-review`, `run-orbit-build-rerun`, `samwise-orbit-build-rerun`, `Orbit-Build-Rerun-Checklist`, `Docs/Orbit/README` | Start future fresh-main Orbit reruns from `samwise-orbit-build-rerun` and keep the rerun-prep/evidence/retrospective chain current between attempts | `swift run personakit validate --root .personakit` passed; `./Scripts/check-worktree-squad-logs.sh` passed |
| 2026-03-10 | Need each squad member to red-pen their own deliverable repeatedly before calling it done | Tightened the generic squad gating contract plus the Orbit rerun playbook, directive, checklist, and rerun-prep note so each active owner must run at least three self red-pen passes before `review-ready` or `MVP candidate` language is allowed | `worktree-squad-gating-contract`, `orbit-build-rerun-playbook`, `run-orbit-build-rerun`, `Orbit-Build-Rerun-Checklist`, `2026-03-09-orbit-foundation-rerun-prep` | Treat repeated owner red-pen passes as required gate evidence for future Orbit reruns and similar squad checkpoints | `swift run personakit validate --root .personakit` passed; `./Scripts/check-worktree-squad-logs.sh` passed |
| 2026-03-10 | Need future Orbit rerun branches to use a simple incrementing integer instead of ad hoc names | Updated the Orbit rerun checklist, Orbit rerun playbook, execution plan, and lane-approval guidance so fresh-main Orbit attempts use `codex/orbit-<n>` naming and preserve the original named lanes only as historical context | `Orbit-Build-Rerun-Checklist`, `orbit-build-rerun-playbook`, `Orbit-Execution-Plan`, `worktree-lane-approvals`, `partner-context-log` | Name the next fresh-main Orbit attempt using the next integer branch, for example `codex/orbit-2`, and keep future attempts incrementing from there | Docs reviewed for branch-name consistency |
| 2026-03-10 | Need the next Orbit rerun staged in docs without implying that kickoff is already approved | Added an attempt-specific prep note for `codex/orbit-1`, clarified that selecting the next integer token does not itself approve or create the lane, and recorded the remaining startup work so Samwise can resume from a stable hold point later | `2026-03-10-orbit-1-rerun-prep`, `Orbit-Build-Rerun-Checklist`, `orbit-build-rerun-playbook`, `worktree-lane-approvals`, `partner-context-log` | Treat `codex/orbit-1` as the staged next attempt token only; do not create or bootstrap the lane until AJ explicitly approves starting the rerun | Docs reviewed for approval-hold consistency; no worktree or branch was created |
| 2026-03-10 | Need the Orbit rerun contract cleanup to preserve the hold state, lock explicit `orbit-1` evidence destinations, and remain continuity-safe | Clarified that `codex/orbit-1` is already the manifest-approved lane while kickoff still waits for AJ start approval, locked concrete `orbit-1` file paths for interaction-quality, validation, participant-evidence, and red-pen artifacts, and kept the Orbit rerun kit session-scoped instead of globally loaded on Samwise | `samwise`, `samwise-orbit-build-rerun`, `orbit-build-rerun-core`, `orbit-build-rerun-review`, `orbit-build-rerun-playbook`, `Orbit-Build-Rerun-Checklist`, `2026-03-10-orbit-1-rerun-prep`, `Orbit-Retrospective-Policy`, `retrospectives/README`, `worktree-lane-approvals` | Start `codex/orbit-1` only after AJ explicitly approves kickoff, then write all new evidence into the locked `2026-03-10-orbit-1-*` artifacts instead of editing `orbit-foundation` records | `swift run personakit validate --root .personakit` passed; `./Scripts/check-worktree-squad-logs.sh` passed; `./Scripts/check-worktree-lane-approvals.sh` passed; `./Scripts/check-gardening-logs.sh` passed |
| 2026-03-10 | Need a real Samwise Orbit execution surface that forces squad routing before implementation and warns about external-worktree approval prompts | Added a dedicated Orbit execution essential, intent, directive, and session so startup and live-lane execution are separate, updated the startup surfaces to hand off into the new execution session, and documented the external-worktree approval-prompt caveat as part of the Orbit rerun contract | `orbit-rerun-squad-execution-map`, `orbit-rerun-squad-execution`, `run-orbit-rerun-squad-execution`, `samwise-orbit-rerun-execution`, `run-orbit-build-rerun`, `orbit-build-rerun-playbook`, `Orbit-Build-Rerun-Checklist`, `Docs/Orbit/README`, `session-directory`, `partner-context-log` | Use `samwise-orbit-build-rerun` to stage the lane, then hand the live Orbit worktree to `samwise-orbit-rerun-execution` so implementation and review are routed through distinct specialist sessions before any success claim | `swift run personakit validate --root .personakit` passed; `./Scripts/check-worktree-squad-logs.sh` passed; `./Scripts/check-gardening-logs.sh` passed; exported `samwise-orbit-rerun-execution` session resolves successfully |
| 2026-03-10 | Need the Samwise review style for PersonaKit sessions to be formal, repeatable, and review-first instead of improvised ad hoc | Added a dedicated session-stack review rubric, intent, directive, and Samwise session so session audits now trace the stack file by file, produce SWOT plus deliverable-confidence scoring, run a red-pen pass, and rescore before any fixes are implemented | `session-stack-review-rubric`, `session-stack-review`, `run-session-stack-red-pen-review`, `samwise-session-stack-review`, `session-directory`, `partner-context-log` | Use `samwise-session-stack-review` when reviewing a PersonaKit session or session stack for enforcement strength, confidence, and smallest-next hardening moves before implementation | `swift run personakit validate --root .personakit` passed; `./Scripts/check-worktree-squad-logs.sh` passed; `./Scripts/check-gardening-logs.sh` passed; exported `samwise-session-stack-review` session resolves successfully |
| 2026-03-10 | Need Samwise session reviews to be MCP-first, fail closed on MCP gaps, and persist durable review artifacts instead of thread-only conclusions | Hardened the existing Samwise session-review workflow around `targetSessionRef`, fixed risk-dimension scoring, safe/caution/unsafe/blocked verdicts, durable review/log paths, and a new MCP session-ref resolution tool so session reviews can normalize id-or-path inputs through PersonaKit before analysis begins | `session-stack-review-rubric`, `session-stack-review-log-contract`, `session-stack-review`, `run-session-stack-red-pen-review`, `samwise-session-stack-review`, `personakit_resolve_session_ref`, `SessionReferenceResolver`, `Docs/MCP/*`, `session-reviews`, `session-stack-reviews.jsonl` | Use `samwise-session-stack-review` with `targetSessionRef`, and if MCP resolution or trace fails, stop with an MCP-gap review artifact and improve MCP before continuing | `swift run personakit validate --root .personakit` passed; MCP tool tests passed; `./Scripts/check-session-stack-review-logs.sh` passed; `./Scripts/check-gardening-logs.sh` passed |
| 2026-03-10 | Need the Orbit startup session to act like a true staging/handoff surface instead of mixing startup and retrospective ownership | Tightened the Orbit startup intent so it requires the rerun-prep path, execution session id, and attempt-specific participant, product, interaction, and validation artifact paths, and removed retrospective review ownership from the startup directive so closeout stays with `samwise-orbit-rerun-execution` | `orbit-build-rerun-review`, `run-orbit-build-rerun`, `samwise-orbit-build-rerun`, `samwise-orbit-rerun-execution` | Use `samwise-orbit-build-rerun` only to stage, validate, and hand off the live lane with explicit startup artifacts before execution begins | `swift run personakit validate --root .personakit` passed; `./Scripts/check-worktree-squad-logs.sh` passed; exported `samwise-orbit-build-rerun` now omits `worktree-squad-retrospective-review` and exposes the required startup artifact paths |
| 2026-03-10 | Need Samwise to scale context through explicit session kit choices instead of loading hiring and orchestration machinery into every export by default | Slimmed the base Samwise persona to the trusted-partner identity layer, moved hiring and orchestration context into explicit session `kitOverrides`, and kept narrow sessions like Orbit startup and session review intentionally lean while preserving broader planning and execution sessions through explicit opt-in kits | `samwise`, `samwise-persona-hiring`, `samwise-persona-hiring-calibration`, `samwise-squad-planning`, `samwise-squad-planning-remediation`, `samwise-worktree-squad-oversight`, `samwise-orbit-rerun-execution` | Treat Samwise as a stable identity plus trusted-partner core; add hiring and orchestration context through the session that actually needs it | `swift run personakit validate --root .personakit` passed; `personakit trace_session samwise-orbit-build-rerun` now resolves only `trusted-partner-core` plus `orbit-build-rerun-core`; `personakit trace_session samwise-session-stack-review` now resolves only `trusted-partner-core`; `personakit trace_session samwise-squad-planning` still resolves hiring and orchestration through explicit session overrides |
| 2026-03-10 | Need Samwise sessions to stop inheriting unrelated planning, hiring, and retrospective context from one broad orchestration override | Split the shared Samwise orchestration boundary into planning, worktree-oversight, and retrospective kits, then rewired the planning, oversight, and Orbit execution sessions to load only the workflow-specific context they actually use | `samwise-planning-core`, `samwise-worktree-oversight-core`, `samwise-retrospective-core`, `samwise-squad-planning`, `samwise-squad-planning-remediation`, `samwise-worktree-squad-oversight`, `samwise-orbit-rerun-execution` | Treat hiring as `persona-hiring-core`, planning as `samwise-planning-core`, live delivery as `samwise-worktree-oversight-core`, and retrospectives as explicit opt-in context instead of one shared Samwise bundle | `swift run personakit validate --root .personakit` passed; session exports now resolve narrower kit stacks for planning, oversight, and Orbit execution |
| 2026-03-10 | Need the Samwise orchestration split records to stay honest about remaining intent-level ballast and avoid re-broadening Orbit execution through retrospective calibration context | Removed `samwise-retrospective-core` from the Orbit execution session so live execution keeps only Orbit-specific and worktree-oversight context, and recorded that the earlier split reduced kit-level scope bleed without fully removing the remaining intent-level ballast inside the oversight stack | `samwise-orbit-rerun-execution`, `samwise-retrospective-core`, `samwise-worktree-squad-oversight`, `partner-context-log`, `pack-gardener-log`, `gardening-events.jsonl`, `gardening-pack-coverage.jsonl` | Treat the Samwise kit split as a real narrowing step, but keep future cleanup focused on intent-level ballast in `gated-worktree-delivery` and related review surfaces rather than claiming the oversight lane is fully lean already | `swift run personakit validate --root .personakit` passed; `./Scripts/check-gardening-logs.sh` passed; exported Orbit execution session no longer loads retrospective calibration context |
| 2026-03-10 | Need coffee mode to wake Samwise from diary and brief context rather than loading planning contracts into the startup ritual by default | Slimmed the coffee intent to load only coffee/trust/constraint essentials, rewired `resume-context` around diary-first plus current brief/objective lookup, and kept partner-context notes only as a fallback when the brief is still unclear | `samwise-coffee-checkpoint`, `run-samwise-coffee-checkpoint`, `partner-context-log`, `pack-gardener-log`, `gardening-events.jsonl`, `gardening-pack-coverage.jsonl` | Treat diary and brief as the default wake-up artifacts for coffee mode; if planning metadata changes later, Samwise can interface with it through those durable artifacts instead of loading planning contracts into the coffee surface | `swift run personakit validate --root .personakit` passed; `./Scripts/check-gardening-logs.sh` passed; exported coffee session no longer loads planning or planning-log contracts directly, while inherited closeout context remains a separate base-kit question |
| 2026-03-10 | Need Orbit execution to expose its specialist session-map contract machine-readably instead of relying on directive prose alone | Hardened the Orbit execution intent so all five specialist session routes are required inputs, added reusable parameter-constraint metadata for distinct session routing, and surfaced that metadata through existing MCP intent outputs while keeping the continuity record honest that invocation-time argument enforcement still needs a future intent-runner path | `orbit-rerun-squad-execution`, `run-orbit-rerun-squad-execution`, `Schema.swift`, `Validator.swift`, `intentTemplate.schema.json`, `MCPTools.swift`, `partner-context-log`, `pack-gardener-log`, `gardening-events.jsonl`, `gardening-pack-coverage.jsonl` | Treat Orbit execution as requiring an explicit five-route specialist map at the contract layer; existing intent tooling now exposes the distinctness rule, while actual supplied-value rejection remains future work until PersonaKit grows an intent invocation surface | `swift run personakit validate --root .personakit` passed; `swift test --filter ValidatorTests` passed; `./Scripts/check-gardening-logs.sh` passed |
| 2026-03-10 | Need closeout to stop acting like ambient Samwise base context for startup and review surfaces | Removed `samwise-daily-closeout` from `trusted-partner-core` so narrow Samwise surfaces keep continuity artifacts without inheriting checkpoint-wrap protocol by side effect; the dedicated closeout session remains explicit through its own closeout directive and intent contract | `trusted-partner-core`, `samwise-daily-closeout`, `samwise-coffee-checkpoint`, `samwise-session-stack-review`, `samwise-orbit-build-rerun`, `partner-context-log`, `pack-gardener-log`, `gardening-events.jsonl`, `gardening-pack-coverage.jsonl` | Treat closeout as a first-class Samwise capability, but keep it explicit through the dedicated closeout session/intent rather than ambient in the base trusted-partner kit; use diary and other continuity artifacts independently where startup/review surfaces only need context input | `swift run personakit validate --root .personakit` passed; `./Scripts/check-gardening-logs.sh` passed; targeted exports confirm coffee, session-review, and Orbit startup no longer resolve `samwise-daily-closeout`, while the dedicated closeout session still does |
| 2026-03-10 | Need historical records to stay interpretable after retiring `samwise-orchestration-core` | Added a tombstone index plus translation rules so historical docs and append-only logs can reference retired artifact IDs without being rewritten, while current-state interpretation points to the workflow-specific successor kits and a refreshed coverage snapshot keeps gardening telemetry aligned with the current graph | `historical-artifact-tombstones`, `samwise-orchestration-core`, `persona-hiring-core`, `samwise-planning-core`, `samwise-worktree-oversight-core`, `samwise-retrospective-core`, `Development/README`, `logs/README`, `gardening-pack-coverage.jsonl` | Resolve retired IDs through the tombstone index, keep historical records append-only, and add new continuity notes instead of rewriting old rows when future artifact retirements happen | `swift run personakit validate --root .personakit` passed; `./Scripts/check-gardening-logs.sh` passed |
| 2026-03-11 | Need startup and execution surfaces to stop carrying planning and tracking mass now that the real planning layer is already isolated | Slimmed `gated-worktree-delivery` to delivery-only essentials, rewrote Orbit startup/execution and Samwise oversight directives around explicit handoff artifacts plus approved checkpoint boundaries, and kept the squad-planning contracts unchanged as the only planning-owned surface | `gated-worktree-delivery`, `orbit-build-rerun-review`, `run-orbit-build-rerun`, `run-orbit-rerun-squad-execution`, `oversee-worktree-squad-delivery`, `run-gated-worktree-delivery-loop`, `samwise-squad-planning`, `samwise-squad-planning-remediation`, `partner-context-log`, `pack-gardener-log`, `gardening-events.jsonl` | Treat startup and execution as handoff-driven surfaces that consume only explicit upstream artifacts; keep milestone tracking and venture-product quality-gate ownership in the venture-product planning/tracking workflows instead of live delivery loops | `swift run personakit validate --root .personakit` passed; `./Scripts/check-worktree-squad-logs.sh` passed; `./Scripts/check-squad-planning-logs.sh` passed; session traces confirm the planning trio still resolves only through the squad-planning sessions while `gated-worktree-delivery` no longer resolves `roadmap-milestone-tracking-standard` or `venture-product-quality-gate` |
| 2026-03-11 | Need commit-message format to stay universal instead of relying on local memory or Samwise-specific discipline | Added a Conventional Commit rule to `tools-and-constraints`, which already resolves through every persona's default kit stack, so commit naming stays part of the shared repo operating surface instead of an implicit habit | `tools-and-constraints`, `partner-context-log`, `pack-gardener-log`, `gardening-events.jsonl` | Treat Conventional Commit format as a repo-wide default for all future commits: prefer `type(scope): summary`, or `type: summary` when no useful scope exists | `swift run personakit validate --root .personakit` passed; `./Scripts/check-gardening-logs.sh` passed |
| 2026-03-11 | Need retrospective contracts to stop broadening active execution stacks before closeout actually begins | Split the mixed worktree squad log contract into active loop logging versus closeout-only retrospective logging, removed retrospective format/template/calibration context from the base worktree execution kit and live execution intents, and kept those closeout contracts on retrospective and Rosie surfaces instead | `worktree-squad-loop-log-contract`, `worktree-squad-retrospective-log-contract`, `worktree-squad-core`, `gated-worktree-delivery`, `orbit-rerun-squad-execution`, `worktree-squad-retrospective-review`, `rosie-retrospective-garden-review`, `run-worktree-squad-retrospective`, `oversee-worktree-squad-delivery`, `pack-gardener-core`, `samwise-retrospective-core`, `partner-context-log`, `pack-gardener-log`, `gardening-events.jsonl`, `gardening-pack-coverage.jsonl` | Treat loop logging as active execution evidence, but load Starfish/template/retrospective schema only on closeout and retrospective-gardening surfaces; execution stacks should route to retrospective sessions rather than carrying closeout machinery by default | `swift run personakit validate --root .personakit` passed; `./Scripts/check-worktree-squad-logs.sh` passed; `./Scripts/check-gardening-logs.sh` passed; session traces confirm `worktree-squad-delivery`, `samwise-worktree-squad-oversight`, and `samwise-orbit-rerun-execution` no longer resolve the retrospective format/template directly while `worktree-squad-retrospective` and Rosie still do |
| 2026-03-11 | Need the retrospective boundary split to stay deterministic at the operator surface and keep calibration on the actual closeout path | Tightened the Orbit execution directive to load only the explicit `retrospectivePath` handoff artifact, restored `worktree-squad-calibration-benchmarks` on the closeout intent, and updated the gated delivery directive to name the loop versus retrospective contracts explicitly during closeout | `run-orbit-rerun-squad-execution`, `worktree-squad-retrospective-review`, `run-gated-worktree-delivery-loop`, `partner-context-log`, `pack-gardener-log`, `gardening-events.jsonl` | Treat retrospective context as explicit closeout input, not a generic execution artifact bucket, and keep calibration available where the real retrospective session runs rather than only in sidecar retrospective tooling | `swift run personakit validate --root .personakit` passed; `./Scripts/check-worktree-squad-logs.sh` passed; session traces confirm `worktree-squad-retrospective` now resolves `worktree-squad-calibration-benchmarks` while Orbit execution wording points to the declared retrospective handoff path |
| 2026-03-11 | Need session lifecycle labels to reflect intended maturity for validated Samwise specialist lanes instead of treating “specialized” as “provisional” | Clarified the lifecycle-state definitions so `active` includes validated specialist lanes and `candidate` means genuinely provisional or not yet promoted, then promoted `samwise-squad-planning-remediation`, `samwise-persona-hiring`, and `samwise-persona-hiring-calibration` to `active` in the session directory | `session-lifecycle-states`, `session-directory`, `samwise-squad-planning-remediation`, `samwise-persona-hiring`, `samwise-persona-hiring-calibration`, `partner-context-log`, `pack-gardener-log`, `gardening-events.jsonl` | Treat validated specialist Samwise lanes as `active` when they are the approved workflow for specific triggers or remediation conditions; reserve `candidate` for lanes that are still provisional or on deck | `swift run personakit validate --root .personakit` passed; `./Scripts/check-gardening-logs.sh` passed |

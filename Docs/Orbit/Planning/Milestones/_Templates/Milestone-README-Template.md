# <Milestone Title>

Status: <Planned | Ready For Planning Closeout | In Review>
Primary Owner: `<primary-persona-id>`
Supporting Personas: `<persona-a>`, `<persona-b>`, `<persona-c>`
Last Updated: <YYYY-MM-DD>

## Purpose

<Explain what this milestone exists to achieve and why it matters in the larger
Orbit sequence.>

## Quality Standard

`<milestone-id>` is not successful because output exists.

`<milestone-id>` is successful only when the result is:

- high quality in the way this milestone specifically requires
- reviewable by the right roles
- strong enough that later milestones can build on it without re-arguing basics

The bare minimum is not a milestone win.

## File Map

- `README.md`
  milestone overview, packet order, and top-level guardrails
- `<supporting-file>.md`
  <purpose>
- `<supporting-file>.md`
  <purpose>

## Preconditions

- <dependency already closed or explicitly staged>
- <decision already frozen>
- <required planning stack alignment>

## Scope Freeze

In scope:

- <in-scope item>
- <in-scope item>
- <in-scope item>

Out of scope:

- <out-of-scope item>
- <out-of-scope item>
- <out-of-scope item>

## Required Inputs

- `<source doc>`
- `<source doc>`
- `<RFC or runtime note>`

## Execution Packets

### Packet 1. <Packet Name>

Outcome:

- <what this packet achieves>

Work:

- <ordered work item>
- <ordered work item>
- <ordered work item>

Done when:

- <clear completion signal>

### Packet 2. <Packet Name>

Outcome:

- <what this packet achieves>

Work:

- <ordered work item>
- <ordered work item>

Done when:

- <clear completion signal>

## Subagent Use Pattern

Safe subagents:

- <subagent use>
- <subagent use>

Avoid:

- <bad delegation or write-heavy pattern>

## Evidence Package

- <required artifact>
- <required artifact>
- <required review result>

## Stop Points

- stop if <named ambiguity or dependency>
- stop if <review gate condition>
- stop if <quality condition is not met>

## Exit And Handoff

Exit when <milestone-specific success statement>.

Handoff forward to:

- `<next milestone or lane>`
- `<next milestone or lane>`

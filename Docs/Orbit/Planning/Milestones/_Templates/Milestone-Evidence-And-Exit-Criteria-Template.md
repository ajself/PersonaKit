# <Milestone Id> Evidence And Exit Criteria

Status: <Draft | Ready For Planning Closeout | In Review | Accepted>
Milestone: `<milestone-id>`
Owner: `<primary-persona-id>`
Last Updated: <YYYY-MM-DD>

## Purpose

Define the proof package required to close `<milestone-id>` honestly.

## Hero Proof

`<milestone-id>` should produce one hero proof that shows the milestone's most
important trust claim in a small, reviewable form.

Example shape:

- <one key scenario>
- <one durable artifact>
- <one visible reviewable result>
- <one validation result>

If that hero proof is weak or incomplete, the milestone is not done.

## Required Artifacts

1. <artifact>
2. <artifact>
3. <artifact>
4. <artifact>

## Exit Checklist

`<milestone-id>` exits only when all of these are true:

- <exit condition>
- <exit condition>
- <exit condition>
- <review pass completed>
- <validation pass completed>

## Residual Open Dependencies

- <real dependency that can still block execution>

Keep real dependencies visible instead of hiding them behind optimistic wording.

## Not Enough To Exit

These do not count as success:

- <thin success claim>
- <thin success claim>
- <thin success claim>

## Review Gate

Before the next milestone broadens scope, reviewers should be able to inspect a
small convincing packet containing:

- <artifact>
- <artifact>
- <artifact>

If that packet is not sharp, the milestone should remain open.

# TODO

- Evaluate replacing filesystem-backed core unit tests (for example tests using `makeTempDirectory()`) with injected in-memory/test-double file-system dependencies where feasible, while preserving deterministic path behavior coverage.

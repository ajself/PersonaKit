# Current State

Status: Active
Owner: AJ + Samwise
Last Reviewed: 2026-03-29

## Purpose

Provide the operator-facing control tower for what is truly active, what is
blocked, what is parked, and where the source-of-truth docs live.

Keep this page short.

Update it only when milestone priorities, review gates, closeout posture, or
explicit reprioritization changes.

## Current Priority

- Orbit is the forward product direction for the repository.
- Most recently closed milestone: `M6` Structured Post Objects And Decision
  Packets.
- Immediate post-`M6` gate resolved: `orbit-memory-gardener` now exists as the
  required owner for later memory-governance work.
- Next likely move: if AJ wants Orbit to keep advancing now, start later `M8`
  packet work from the accepted `M8-P1` baseline under
  `orbit-memory-gardener`.
- Priority rule: do not broaden into later `M8` packets unless AJ explicitly
  starts that next slice.

## Blocked Or Gated Work

- `M8` planning baseline exists and is no longer persona-blocked, but later
  packets remain unstarted beyond accepted `M8-P1`.
- Taskboard `NS0` remains pair-required and incomplete because the real
  interactive evidence/report loop is still open.

## Parked Initiative With Open Checkpoint

- Taskboard is not the active forward program.
- Its one explicit open checkpoint is the real `NS0` paired evidence run plus
  the follow-up readiness memo.

## Source-Of-Truth Map

- Orbit roadmap and milestone dossiers live in `Docs/Orbit/Planning/`.
- PersonaKit operational rules, logs, and workflow guidance live in
  `Docs/PersonaKit/Development/`.
- Historical PersonaKit plans and research, including Taskboard planning,
  live in `Docs/Archive/PersonaKit/`.
